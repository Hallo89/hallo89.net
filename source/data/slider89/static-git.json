[
  {
    "html-url": "https://github.com/Hallo89/Slider89/releases/tag/v0.2.0",
    "tag_name": "v0.2.0",
    "body": "<ul>\n<li>New property: <a href=\"#structure\"><code>structure</code></a>. It takes a string similar to an XML structure to assemble a customized HTML structure</li>\n<li><a href=\"#element\"><code>element</code></a> has been changed in favor of the new structure system: It is now a JavaScript object containing every part of the slider as opposed to only the root element</li>\n<li><a href=\"#tipDuration\"><code>tipDuration</code></a> can now be set to <code>false</code> to prevent it from disappearing altogether</li>\n<li>The HTML classing has been changed to improve stylability: non crucial CSS suited for altering, has been moved into a new, independent class to be spoken to without nesting in the selector, <code>slider89_{slider-part}</code>; crucial CSS is still selected with a (more difficult to override) <code>.slider89 .slider_{slider-part}</code></li>\n</ul>\n",
    "date": "31/12/2018",
    "new": false
  },
  {
    "html-url": "https://github.com/Hallo89/Slider89/releases/tag/v0.1.1",
    "tag_name": "v0.1.1",
    "body": "<ul>\n<li>New property: <a href=\"#taskMouseUp\"><code>taskMouseUp</code></a>, taking a function to be executed every time the mouse is released after engaging with a slider possessing this property</li>\n<li><a href=\"#value\"><code>value</code></a> is now trimmed to the value of <a href=\"#comma\"><code>comma</code></a>, when specified, within the same initialization</li>\n<li>If <code>value</code> is changed to more than <a href=\"#max\"><code>max</code></a> or less than <a href=\"#min\"><code>min</code></a> with <a href=\"#newValues\"><code>newValues()</code></a>, they are now set to min and max accordingly</li>\n<li>Fixed the width not computing anew when changing values with <code>newValues()</code></li>\n<li>Fixed new <code>min</code>, <code>max</code> and <code>comma</code>  values being ignored when they are changed to 0 in <code>newValues()</code></li>\n</ul>\n",
    "date": "8/12/2018",
    "new": false
  },
  {
    "html-url": "https://github.com/Hallo89/Slider89/releases/tag/v0.1.0",
    "tag_name": "v0.1.0",
    "body": "<p>Slider89's initial release - more updates will come in the future.</p>\n",
    "date": "11/11/2018",
    "new": false
  }
]
